# Server Configuration
NODE_ENV=development
PORT=3000
HOST=localhost

# Database Configuration
# To use the provided Atlas cluster, set MONGODB_URI to the SRV string below.
# You can override username/password/host as needed.
MONGODB_URI=mongodb+srv://estianstandergit_db_user:gSaZmEv47a6IuoyZ@baymaster.ftqcdzf.mongodb.net/

# Job Costing DB (separate from BayMaster app DB)
# If set, Job Costing saves to this URI + DB name below.
JOBS_MONGODB_URI=mongodb+srv://stormfoxstudi_db_user:WjZLMVHXEpfSdtAU@inductotrackdb.q5ekljf.mongodb.net/
JOBS_DB_NAME=inductotrackdb

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-refresh-token-secret
JWT_REFRESH_EXPIRE=30d

# Security
BCRYPT_ROUNDS=10
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=logs/app.log

# Email Configuration (Optional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=

# External APIs (Optional)
API_KEY=
API_SECRET=
